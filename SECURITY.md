# Security and privacy

## 报告安全问题

仓库公开后，请使用 GitHub Private Vulnerability Reporting 或直接联系仓库所有者，不要在公开 Issue 中披露可利用的注入、任意文件写入或敏感信息泄露问题。

## 提交日志

日志可能包含本机目录、Windows 用户目录、存档文件名、玩家名称或联机信息。提交前请先脱敏，并且不要上传未经授权的 `.eu4` 存档、配置目录或数据库。

## 原生桥边界

原生桥会向 `eu4.exe` 注入 DLL，仅对 `491d` 完成过实机验证。非 `491d` 风险模式不是兼容性保证，也不是安全保证。

