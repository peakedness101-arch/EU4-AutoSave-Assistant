# 491d 联机客机剩余验收

已完成的真实联机证据：两次客机本地保存均返回 `native_result=0`，生成的 ZIP 存档可解析，`savegame_version=1.37.5.0`、`multi_player=yes`，体感卡顿约半秒；两份文件随后完成哈希校验、改名和移动。

下次具备联机客机环境后，只需补做以下低风险验收：

1. 双击根目录 `EU4_AutoSave_Assistant.exe`，用地图左上角按钮收起并重新展开左侧功能栏，确认其中显示构建标识 `491d`，再点击“连接原生桥”。导入当前存档后，确认政治地图按国家颜色显示；按住鼠标左键可拖动，滚轮可缩放，右侧国家栏可收起并恢复。
2. 单击一个省份，确认右侧切换到该省原主国家；单击有军队的省份，确认军队名称、团数和实际兵力出现在省份旁浮窗。找一个被占领省份，确认原主底色上叠加控制方斜纹。
3. 选择“每 3 个游戏月”，跨过一个季度边界；确认仅生成一次存档，归档小窗依次出现“存档验证成功”和“自动归档完成”。随后确认政治地图、军队标记与右侧警告详情均切换到新存档。
4. 选择“每 10 个现实分钟”，正常游玩至少 20 分钟；确认约产生两份存档，不出现并发请求或重复归档。
5. 退出并重新启动游戏，再次进入客机联机战局；确认无需重启助手即可点击“连接原生桥”重新加载。
6. 持续运行 30–60 分钟，检查归档页会话日志与归档目录，确认没有漏存、重复、超时或残留的 `c` 加 13 位时间戳源文件；在此期间反复拖动、缩放地图和切换国家，确认界面保持响应。

若保存失败，当前季度或年度不会被记为完成；程序会在 30 秒冷却后重试。自动归档会等保存文件稳定并通过完整解析后才开始，不会移动普通手动存档。
